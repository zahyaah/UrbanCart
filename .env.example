VITE_API_URL=http://localhost:3001

# Stripe test-mode PUBLISHABLE key (safe to expose client-side; never the
# secret key). https://dashboard.stripe.com/test/apikeys
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
